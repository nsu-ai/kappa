stages:
  - test

.rust-ci:
  stage: test
  image: rust:latest
  before_script:
    - rustc --version
    - cargo --version
  script:
    - cargo test
    - cargo clippy -- -D warnings

rust-test:
  extends: .rust-ci
